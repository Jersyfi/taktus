# build
dist/
build/
*.egg-info/

# generated — see docs/architecture/project-structure.md; api/openapi.yaml is generated and
# committed, so that the interface can be read without running anything, and a test holds it
# to `make generate`
src/taktus/shared/generated/
web/build/
node_modules/

# environment — never commit a secret value
.env
.env.*
!.env.example
*.pem
*.key
kubeconfig*

# tooling
.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.DS_Store
