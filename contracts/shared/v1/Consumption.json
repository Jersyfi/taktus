{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://taktus.eu/contracts/shared/v1/Consumption.json",
  "title": "Consumption",
  "description": "The raw quantities one step used or is expected to use. Nothing here is converted into money; the control plane derives the normalised Takt from these quantities (docs/architecture/accounting.md). At least one quantity is present. compute_seconds is meaningless without its resource_class.",
  "type": "object",
  "allOf": [{ "$ref": "#/$defs/quantities" }, { "$ref": "#/$defs/atLeastOneQuantity" }],
  "unevaluatedProperties": false,
  "$defs": {
    "quantities": {
      "title": "ConsumptionQuantities",
      "description": "The measurable quantities, without closure, so that other schemas can extend them.",
      "type": "object",
      "properties": {
        "tokens_in": {
          "description": "Language-model input tokens.",
          "type": "integer",
          "minimum": 0
        },
        "tokens_out": {
          "description": "Language-model output tokens.",
          "type": "integer",
          "minimum": 0
        },
        "currency": { "$ref": "#/$defs/currencyAmounts" },
        "quota_units": {
          "description": "Units of a subscription window, in the unit the worker declares in its capabilities.",
          "type": "number",
          "minimum": 0
        },
        "compute_seconds": {
          "description": "Seconds of compute held, in the given resource class.",
          "type": "number",
          "minimum": 0
        },
        "resource_class": { "$ref": "#/$defs/resourceClass" },
        "storage_bytes": {
          "description": "Bytes of persistent state written.",
          "type": "integer",
          "minimum": 0
        }
      },
      "dependentRequired": {
        "compute_seconds": ["resource_class"]
      }
    },
    "atLeastOneQuantity": {
      "anyOf": [
        { "required": ["tokens_in"] },
        { "required": ["tokens_out"] },
        { "required": ["currency"] },
        { "required": ["quota_units"] },
        { "required": ["compute_seconds"] },
        { "required": ["storage_bytes"] }
      ]
    },
    "currencyAmounts": {
      "title": "CurrencyAmounts",
      "description": "Money by ISO 4217 code in lowercase, for example {\"eur\": 3.2}. A map rather than a fixed currency so that no currency is baked into the contract.",
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z]{3}$" },
      "additionalProperties": { "type": "number", "minimum": 0 }
    },
    "resourceClass": {
      "title": "ResourceClass",
      "description": "A class of compute, named by the operator: cpu.small, gpu.small, accelerator.large. The weighting table maps classes to Takt.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(\\.[a-z0-9-]+)*$"
    }
  }
}
