{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://taktus.eu/contracts/shared/v1/LedgerEntry.json",
  "title": "LedgerEntry",
  "description": "One link of the content-free hash chain (ADR-0006). The entry references what happened and where the content is; it stores no payload, no personal data and no secret. The ledger is the single source for every metric. The first entry of a chain has prev_hash null.",
  "type": "object",
  "properties": {
    "seq": { "type": "integer", "minimum": 1 },
    "ts": { "type": "string", "format": "date-time" },
    "kind": {
      "description": "What happened, as a lowercase dotted token: run.started, step.finished, decision.confirmed, assignment.rejected. Three kinds record that a result left the system (ADR-0022): egress.write (a connector wrote outward), egress.delivery (a channel delivered), egress.read (an external system read through Taktus); each references what went out in refs.artifact_ids or content_digest.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$"
    },
    "prev_hash": {
      "description": "Hash of the previous entry; null only for the first entry.",
      "oneOf": [{ "$ref": "Artifact.json#/$defs/digest" }, { "type": "null" }]
    },
    "hash": {
      "description": "Hash over this entry without the hash field itself, computed as the ledger component documents.",
      "$ref": "Artifact.json#/$defs/digest"
    },
    "refs": {
      "description": "What the entry is about. Identifiers only.",
      "type": "object",
      "properties": {
        "tenant": { "type": "string", "minLength": 1 },
        "command_id": { "type": "string", "minLength": 1 },
        "plan_id": { "type": "string", "minLength": 1 },
        "process_version": { "type": "string", "minLength": 1 },
        "run_id": { "type": "string", "minLength": 1 },
        "step_id": { "type": "string", "minLength": 1 },
        "assignment_id": { "type": "string", "minLength": 1 },
        "decision_request_id": { "type": "string", "minLength": 1 },
        "artifact_ids": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "actor": {
          "description": "The identity that acted, as an opaque identifier. Absent when Taktus acted on its own.",
          "type": "string",
          "minLength": 1
        }
      },
      "minProperties": 1,
      "additionalProperties": false
    },
    "method": { "$ref": "Method.json" },
    "model": {
      "description": "The model used, pinned as name@version.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._-]*@[A-Za-z0-9][A-Za-z0-9._+-]*$"
    },
    "adapter": {
      "description": "Identifier of the adapter configuration that executed. Configuration maps it to a product; the ledger does not.",
      "type": "string",
      "minLength": 1
    },
    "consumption": { "$ref": "Consumption.json" },
    "outcome": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "content_digest": {
      "description": "Digest of the referenced content, so that the reference is verifiable without the content.",
      "$ref": "Artifact.json#/$defs/digest"
    }
  },
  "required": ["seq", "ts", "kind", "prev_hash", "hash", "refs"],
  "additionalProperties": false
}
