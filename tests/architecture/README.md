# Architecture tests

The adapter obligation, the component boundaries and the ban on product names in the core, as
tests (ADR-0003, ADR-0016). Filled from `0.1.0`; until then `make gate-arch` reports "no targets yet".
